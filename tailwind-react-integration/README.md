# Tailwind React Integration

Minimal project showing Tailwind CSS integration with Vite + React and a `UserProfile` component.

Run:

```bash
npm install
npm run dev
```

The `UserProfile` component demonstrates responsive design and interactivity using Tailwind CSS utility classes.
